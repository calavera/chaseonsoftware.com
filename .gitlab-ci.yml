image: node:4.2.2

stages:
  - deploy

pages:
  stage: deploy
  script:
    - ./script/cibuild-pre
    - cd themes/minimalist && npm run build && cd ../../
    - ./script/build
  artifacts:
    paths:
      - public
  only: 
    - master
