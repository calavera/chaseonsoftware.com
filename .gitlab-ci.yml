image: node:4.2.2

stages:
  - deploy

pages:
  stage: deploy
  script:
    - make cibuild-pre
    - cd themes/minimalist && npm run build && cd ../../
    - make build
  artifacts:
    paths:
      - public
  only: 
    - master
