extends ../master

  block meta
    meta[name="description"][content=$.Description]
    
  block title
    | #{$.Title}

  block content
    div.post.page
      h1 #{$.Title}
      if $.Params.description
        div.purpose #{$.Params.description}
      div #{$.Content}
      
      if $.Params.updated
        div Last Updated: #{$.Params.updated}
