extends ../master

  block content
    ul.collection
      each $post in $.Site.Pages
          if $.Type == "article" 
            li 
              a[href=$.Permalink] #{$.Title}
          if $.Type == "external"
            li 
              a[href=$.Params.link] #{$.Title} 
              span (external) 
  
