extends ../master

  block content
    ul.collection
      each $post in $.Data.Pages
        li 
          a[href=$.Permalink] #{$.Title} 
          if $.Params.purpose
            div #{$.Params.purpose}
